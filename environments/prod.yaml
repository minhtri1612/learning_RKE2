# ─── LAYER 1 – DEVELOPER ───
# Chỉ sửa version để deploy / rollback.
# PROD: test kỹ trên dev trước khi đổi version ở đây.

env: prod
project: prod
cluster: prod
automated: false

services:
  backend:
    version: v1.2.5

  database:
    version: v1.2.4
