# ─── LAYER 1 – DEVELOPER ───
# DEV: có thể dùng branch (kind, dev) hoặc tag. Đổi version → deploy/rollback.

env: dev
project: dev
cluster: dev
automated: true

services:
  backend:
    version: kind   # branch hoặc tag → targetRevision + image tag

  database:
    version: kind   # targetRevision cho chart database
