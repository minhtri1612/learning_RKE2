# Config theo môi trường: prod
env: prod
project: prod
clusterName: prod
valueFileSuffix: prod
# Khi promote, targetRevision nên là tag/commit đã được QA (ví dụ: v1.2.3), không bám trực tiếp main.
targetRevision: v1.2.3
