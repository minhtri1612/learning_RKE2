# ─── LAYER 1 – DEVELOPER ───
# Dev: chỉ sửa version để deploy/rollback từng component độc lập.
# version = git targetRevision + Docker image tag (backend).

env: dev
project: dev
cluster: dev
automated: true

services:
  backend:
    version: v3.0.1   # targetRevision + image: minhtri1612/rke2:v3.0.1

  database:
    version: v3.0.1   # targetRevision cho chart database
