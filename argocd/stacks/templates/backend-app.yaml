{{/*
ArgoCD Application cho Backend.
"Tách biệt từng mẫu" — file riêng cho backend.
Nộp vào _generic_application.tpl để sinh Application object.
*/}}
{{- include "stacks.application" (dict "name" "backend" "root" $) }}
