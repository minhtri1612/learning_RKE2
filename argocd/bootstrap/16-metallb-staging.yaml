apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metallb-staging
  namespace: argocd
spec:
  project: staging
  sources:
    - repoURL: https://metallb.github.io/metallb
      chart: metallb
      targetRevision: 0.15.3
      helm:
        releaseName: metallb
        valueFiles:
          - $values/config/metallb/metallb-common.yaml
    - repoURL: https://github.com/minhtri1612/learning_RKE2.git
      targetRevision: main
      ref: values
    - repoURL: https://github.com/minhtri1612/learning_RKE2.git
      path: config/metallb/pools/staging
      targetRevision: main
  destination:
    name: staging
    namespace: metallb-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
