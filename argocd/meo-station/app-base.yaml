# ─── XƯƠNG SỐNG APP (base) ───
# DevOps định nghĩa chartPath, namespace, syncWave.
# Cấu trúc đã được chuyển qua mô hình IDP 3 Layer.

apps:
  backend:
    name: backend
    chartPath: 1-platform-engine/generic-app
    namespace: meo-stationery
    targetRevision: main
    syncWave: "1"

  database:
    name: database
    chartPath: 1-platform-engine/generic-app
    namespace: database
    targetRevision: main
    syncWave: "0"
