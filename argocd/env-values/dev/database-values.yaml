# Dev overrides (merged with k8s_helm/database/values.yaml)
existingSecret:
  name: "meo-stationery-database-secrets-dev"
auth:
  username: meo_admin
  database: meo_stationery
  password: ""  # password lấy từ Secret, không set trong Git

persistence:
  useEBS: false

replicas: 1

