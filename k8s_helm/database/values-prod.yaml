# Prod overrides (merged with values.yaml)
existingSecret:
  name: "postgres"
persistence:
  size: 10Gi
  useEBS: true
  provisionStorageClass: false
auth:
  username: meo_admin
  database: meo_stationery
