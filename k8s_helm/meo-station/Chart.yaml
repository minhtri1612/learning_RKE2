apiVersion: v2
name: meo-station
description: Umbrella chart — deploy backend + database (subcharts), không sinh Application CRD
type: application
version: 0.3.0
appVersion: "1.0.0"
dependencies:
  - name: database
    version: "0.1.0"
    repository: "file://../database"
  - name: backend
    version: "0.1.0"
    repository: "file://../backend"
