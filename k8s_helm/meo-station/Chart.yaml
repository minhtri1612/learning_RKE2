apiVersion: v2
name: meo-station
description: App-of-Apps template — sinh Application CRD (database, backend), không dependencies
type: application
version: 0.2.0
appVersion: "1.0.0"
# Không dependencies: chart chỉ render Application CRD, không render Pod/Service trực tiếp
