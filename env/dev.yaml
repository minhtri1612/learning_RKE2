# Merge: helm ... -f app/be.yaml|db.yaml -f env/dev.yaml

env: dev
useExternalSecrets: true

backend:
  image:
    tag: "2.0.3"
  replicaCount: 4
database:
  image:
    tag: "16"
  replicaCount: 3
frontend:
  image:
    tag: "1.0.0"
  replicaCount: 5

