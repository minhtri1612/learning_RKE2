# AUTO — scripts/kind-clustermesh-peer-ip.sh — IP mạng Docker kind của management-control-plane
clustermesh:
  config:
    clusters:
      management:
        port: 32379
        ips:
          - 172.18.0.2
