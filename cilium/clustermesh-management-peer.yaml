# Spoke → hub ClusterMesh. Placeholder được ./configure.py (management / dev / prod) patch từ terraform output management.
clustermesh:
  config:
    clusters:
      - name: management
        port: 32379
        ips:
          - PLACEHOLDER_MGMT_PRIVATE_IP
