# Environment variables
.env
.env.local
.env*.local

# Dependencies
/node_modules/
/.next/

# IDE
/.idea/
.vscode/
*.swp
*.swo
*~

# Python
__pycache__/
*.pyc

# Misc
natehigger
RKE2_REVIEW_SUMMARY.md
check_status.sh

# Kubernetes - Secrets and configs
.kube/
kubeconfig
rke2.yaml
kube_config_rke2.yaml
.kube_config_rke2_tunnel.yaml

# OpenVPN client configs (chứa private key)
*.ovpn
# OpenVPN systemd service (sinh bởi deploy.py, có path tuyệt đối)
openvpn-practice-rke2.service
# Ansible static inventory (sinh bởi deploy.py, IP từ Terraform output)
ansible/inventory_openvpn.yml

# Terraform - CRITICAL: Contains secrets and state
*.tfstate
*.tfstate.backup
*.tfstate.*
tfplan
tfplan.*
.terraform/
.terraform.lock.hcl
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/k8s-key.pem
terraform/*.pem
terraform/*.key
*.pem
*.key
# Terraform vars with secrets (rke2 token, etc.)
*.tfvars
*.auto.tfvars
terraform/*.tfvars
terraform/*.auto.tfvars
# Environment-specific state and keys
terraform/environments/**/.terraform/
terraform/environments/**/k8s-key.pem
terraform/environments/**/*.tfstate
terraform/environments/**/*.tfstate.*

# Ansible - Contains IPs and connection info
ansible/inventory.ini
ansible/.vault_pass
ansible/group_vars/users.yml
ansible/group_vars/vpn_server.yml

# Helm values with secrets (override with -f values-secrets.yaml, keep out of repo)
**/values-*-secrets.yaml
**/values-secrets.yaml

# Prisma (if you need it, remove this line)
/prisma/