env: dev

projectName: meo-stationery
awsBackendSecretKey: meo-stationery/dev/app-credentials-v2
awsDatabaseSecretKey: /meo-stationery/dev/database

backend:
  refreshInterval: 5m   # shorter so secret updates (e.g. after tf apply) propagate quickly
database:
  refreshInterval: 1h
