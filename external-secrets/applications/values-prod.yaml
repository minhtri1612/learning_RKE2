env: prod

projectName: meo-stationery
awsBackendSecretKey: meo-stationery/prod/app-credentials
awsDatabaseSecretKey: meo-stationery/prod/app-credentials

backend:
  refreshInterval: 1h
database:
  refreshInterval: 1h
