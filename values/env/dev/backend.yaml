replicaCount: 3

ingress:
  host: meo-stationery-dev.local

databaseConnection:
  host: dev-meostation-database-app-generic-app.database.svc.cluster.local

autoscaling:
  minReplicas: 3
  maxReplicas: 5

existingSecret:
  name: meo-stationery-backend-secrets-dev

workload:
  image:
    tag: "3.0.1"
    pullPolicy: Always
