# Base runtime config cho backend — giống nhau mọi env.
# Override per env ở config/env/{env}.yaml

runtimeConfig:
  data:
    featureFlags:
      enableMetrics: true
      maintenanceMode: false

    cache:
      host: redis.default.svc.cluster.local
      port: 6379
