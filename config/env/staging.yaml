# STAGING override — chỉ phần khác so với config/base/config.yaml

backend:
  secrets:
    target:
      name: meo-stationery-backend-secrets-staging
    remoteKey: meo-stationery/staging/app-credentials

database:
  secrets:
    target:
      name: meo-stationery-database-secrets-staging
    remoteKey: meo-stationery/staging/app-credentials
