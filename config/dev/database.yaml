# Runtime config cho database (PostgreSQL) ở môi trường DEV.
# Mount vào pod tại /etc/postgresql/extra.conf

runtimeConfig:
  data:
    max_connections: 20
    shared_buffers: "128MB"
    work_mem: "4MB"
    log_min_duration_statement: 0        # log MỌI query (debug)
    log_statement: "all"
