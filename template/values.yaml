# Default values for template.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: nginx
  # This sets the pull policy for images.
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

# This is for the secrets for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created.
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account.
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template.
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  annotations: {}
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 80

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
    # - secretName: chart-example-tls
    #   hosts:
    #     - chart-example.local

# -- Expose the service via gateway-api HTTPRoute
# Requires Gateway API resources and suitable controller installed within the cluster
# (see: https://gateway-api.sigs.k8s.io/guides/)
gatewayAPI:
  enabled: false
  createGatewayClass: false
  gatewayClassName: cilium
  gatewayClassControllerName: io.cilium/gateway-controller
  createGateway: true
  gatewayName: meo-gw
  gatewayNamespace: ""
  listener:
    name: http
    protocol: HTTP
    port: 80
    allowedRoutesFrom: Same

httpRoute:
  # HTTPRoute enabled.
  enabled: false
  # HTTPRoute annotations.
  annotations: {}
  # Which Gateways this Route is attached to.
  parentRefs:
  - name: gateway
    sectionName: http
    # namespace: default
  # Hostnames matching HTTP header.
  hostnames:
  - chart-example.local
  # List of rules and filters applied.
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /headers
    # Optional explicit backend selection when rollout is enabled:
    # routeTo: canary | stable | preview
    # If omitted, chart uses default behavior (weighted canary split or active service).
  #   filters:
  #   - type: RequestHeaderModifier
  #     requestHeaderModifier:
  #       set:
  #       - name: My-Overwrite-Header
  #         value: this-is-the-only-value
  #       remove:
  #       - User-Agent
  # - matches:
  #   - path:
  #       type: PathPrefix
  #       value: /echo
  #     headers:
  #     - name: version
  #       value: v2
  # -- When rollout is enabled, auto-route to stable/canary services.
  useRolloutCanaryBackends: true
  stableWeight: 90
  canaryWeight: 10

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /
    port: http
readinessProbe:
  httpGet:
    path: /
    port: http

# This section is for setting up autoscaling more information can be found here: https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: []
  # - name: foo
  #   secret:
  #     secretName: mysecret
  #     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
  # - name: foo
  #   mountPath: "/etc/foo"
  #   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}

# Runtime app config (non-sensitive values).
# Use external-secrets for sensitive values (passwords, tokens, secrets).
runtimeConfig:
  enabled: false
  fileName: app.yaml
  mountPath: /app/config
  data: {}
# -- Toggle to use External Secrets for sensitive values
# When enabled, templates will pull secrets from K8s Secret objects managed by ESO (or static Secrets with matching names).
# Default true for Argo deploys; local tests without secrets: --set useExternalSecrets=false or a small values file.
useExternalSecrets: true

# -- Environment name (dev, prod)
env: ""

# -- Deployment strategy/kind (deployment, statefulset, rollout).
# Default to deployment if not set.
workloadKind: deployment

# -- Argo Rollouts config (only used if workloadKind is rollout).
rollout:
  enabled: false
  # -- Per-service override is supported via <service>.rollout (e.g. backend.rollout.strategy).
  # -- Strategy to use: canary or blueGreen.
  strategy: canary
  revisionHistoryLimit: 3
  # -- Canary strategy configuration.
  canary:
    stableService: ""
    canaryService: ""
    maxSurge: 25%
    maxUnavailable: 0
    steps:
      - setWeight: 20
      - pause:
          duration: 60s
      - setWeight: 50
      - pause:
          duration: 120s
      - setWeight: 100
  # -- Blue-Green strategy configuration.
  blueGreen:
    activeService: ""
    previewService: ""
    autoPromotionEnabled: true
    # -- Seconds to wait before scaling down the old ReplicaSet.
    scaleDownDelaySeconds: 30
  # -- Automated analysis templates.
  analysis:
    enabled: false
    templates: []

# -- Optional Cilium L7 network policy (for clusters with Cilium CRDs installed).
ciliumNetworkPolicy:
  enabled: false
  authenticationMode: ""
  ingress:
    fromEndpoints: []
    http: []
  egress:
    toEndpoints: []
    toEntities: []
    port: ""
    protocol: TCP
    http: []
